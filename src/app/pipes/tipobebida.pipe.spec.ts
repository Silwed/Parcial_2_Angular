import { TipobebidaPipe } from './tipobebida.pipe';

describe('TipobebidaPipe', () => {
  it('create an instance', () => {
    const pipe = new TipobebidaPipe();
    expect(pipe).toBeTruthy();
  });
});
